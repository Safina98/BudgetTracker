package com.example.budgettracker2.database

import androidx.room.*

/*
@Entity(tableName = "transaction_table",
    foreignKeys = [ForeignKey(entity = KategoryTable::class,
        parentColumns = ["category_id"],
        childColumns = ["trans_category_id"],
        onDelete = ForeignKey.CASCADE,
        onUpdate = ForeignKey.CASCADE)])
data class TabelTransaksi(
    @PrimaryKey(autoGenerate = true)
    var transaction_id:Int=0,
    @ColumnInfo(name = "cath_id")
    var trans_category_id:Int=0,
    @ColumnInfo(name = "note")
    var note:String="",
    @ColumnInfo(name = "date")
    var date: String="",
    @ColumnInfo(name = "nominal")
    var nominal:Int=0
)

 */
